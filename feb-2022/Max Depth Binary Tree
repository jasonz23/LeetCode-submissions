class Solution {
public:
    int maxDepth(TreeNode* root) {
        if (root == nullptr) {
            return 0;
        }
        int int1 = maxDepth(root->left);
        int int2 = maxDepth(root->right);
        return 1 + max(int1,int2);
    }
};
